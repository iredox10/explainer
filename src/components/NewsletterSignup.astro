---

---

<section class="bg-[#008751] py-16">
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex flex-col items-center text-center">
            <h2 class="text-3xl font-black text-white sm:text-4xl">
                Get Smarter Every Week
            </h2>
            <p class="mt-4 max-w-xl text-lg text-white/90">
                Join thousands of curious readers. We'll send you one visual
                explainer every week that makes complex topics simple.
            </p>
            <form class="mt-8 flex w-full max-w-md flex-col gap-3 sm:flex-row">
                <input
                    type="email"
                    placeholder="Enter your email"
                    class="flex-1 rounded-lg border-2 border-white bg-white px-4 py-3 text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-[#008751]"
                    required
                />
                <button
                    type="submit"
                    class="rounded-lg bg-white px-6 py-3 font-semibold text-[#008751] transition-colors hover:bg-gray-100"
                >
                    Subscribe
                </button>
            </form>
            <p class="mt-4 text-sm text-white/80">
                No spam. Unsubscribe anytime.
            </p>
        </div>
    </div>
</section>
